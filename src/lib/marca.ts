/**
 * El nombre del gimnasio, en un solo lugar: lo muestran el login y el menú.
 * El título de la pestaña vive en `index.html`, que no puede importar esto:
 * si cambia el nombre, se cambia en los dos.
 */
export const NOMBRE_GIMNASIO = 'Athletics';
